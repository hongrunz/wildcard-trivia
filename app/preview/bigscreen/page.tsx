'use client';

import BigScreenDisplay from '../../../frontend/components/BigScreenDisplay';

export default function BigScreenDisplayPreviewPage() {
  // Note: This preview requires a valid roomId to function properly
  return <BigScreenDisplay roomId="preview" />;
}
